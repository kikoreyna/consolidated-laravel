@extends('layouts.app')
@section('content')<div class="container"><div class="d-flex justify-content-between align-items-center mb-3"><h2>Oficinas de transportadoras</h2><a href="{{ route('oficinas.create') }}" class="btn btn-primary">Nueva oficina</a></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-striped mb-0"><thead><tr><th>Transportadora</th><th>Oficina</th><th>Contacto</th><th>Teléfono</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>@forelse($oficinas as $oficina)<tr><td>{{ optional($oficina->transportadora)->nombre ?? 'Sin transportadora' }}</td><td>{{ $oficina->nombre }}</td><td>{{ $oficina->contacto ?? 'N/A' }}</td><td>{{ $oficina->telefono ?? 'N/A' }}</td><td>{{ $oficina->activa ? 'Activa' : 'Inactiva' }}</td><td><a href="{{ route('oficinas.show', $oficina) }}" class="btn btn-sm btn-info">Ver</a> <a href="{{ route('oficinas.edit', $oficina) }}" class="btn btn-sm btn-warning">Editar</a>@if($oficina->activa)<form action="{{ route('oficinas.destroy', $oficina) }}" method="POST" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-danger" onclick="return confirm('¿Desactivar oficina?')">Desactivar</button></form>@endif</td></tr>@empty<tr><td colspan="6" class="text-center">No hay oficinas registradas.</td></tr>@endforelse</tbody></table></div></div></div></div>@endsection
