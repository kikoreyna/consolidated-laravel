@extends('layouts.app')
@section('content')<div class="container"><div class="d-flex justify-content-between align-items-center mb-3"><h2>Coberturas</h2><a href="{{ route('coberturas.create') }}" class="btn btn-primary">Nueva cobertura</a></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="card"><div class="card-body"><table class="table table-striped"><thead><tr><th>Cliente</th><th>Modalidad</th><th>Oficina</th><th>Bodega</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>@forelse($coberturas as $cobertura)<tr><td>{{ optional($cobertura->cliente)->nombre }}</td><td>{{ $cobertura->modalidad_entrega === 'ocurre' ? 'A ocurre' : 'A domicilio' }}</td><td>{{ optional($cobertura->oficina)->nombre ?? 'N/A' }}</td><td>{{ optional($cobertura->bodega)->nombre ?? 'N/A' }}</td><td>{{ $cobertura->activa ? 'Activa' : 'Inactiva' }}</td><td><a href="{{ route('coberturas.edit', $cobertura) }}" class="btn btn-sm btn-warning">Editar</a>@if($cobertura->activa)<form action="{{ route('coberturas.destroy', $cobertura) }}" method="POST" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Desactivar</button></form>@endif</td></tr>@empty<tr><td colspan="6" class="text-center">No hay coberturas registradas.</td></tr>@endforelse</tbody></table></div></div></div>@endsection
