@extends('layouts.app')
@section('content')<div class="container"><div class="d-flex justify-content-between align-items-center mb-3"><h2>Destinatarios</h2><a href="{{ route('destinatarios.create') }}" class="btn btn-primary">Nuevo destinatario</a></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-striped mb-0"><thead><tr><th>Nombre</th><th>Contacto</th><th>Teléfono</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>@forelse($destinatarios as $destinatario)<tr><td>{{ $destinatario->nombre }}</td><td>{{ $destinatario->contacto ?? 'N/A' }}</td><td>{{ $destinatario->telefono ?? 'N/A' }}</td><td>{{ $destinatario->activo ? 'Activo' : 'Inactivo' }}</td><td><a href="{{ route('destinatarios.show', $destinatario) }}" class="btn btn-sm btn-info">Ver</a> <a href="{{ route('destinatarios.edit', $destinatario) }}" class="btn btn-sm btn-warning">Editar</a>@if($destinatario->activo)<form action="{{ route('destinatarios.destroy', $destinatario) }}" method="POST" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-danger" onclick="return confirm('¿Desactivar destinatario?')">Desactivar</button></form>@endif</td></tr>@empty<tr><td colspan="5" class="text-center">No hay destinatarios registrados.</td></tr>@endforelse</tbody></table></div></div></div></div>@endsection
